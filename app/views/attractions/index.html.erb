<% @attractions.each do |attraction| %>
<p><%= "#{attraction.name}" %> <%= link_to "Go on #{attraction.name} ", attraction_path(attraction) unless @user.admin %>
<%= link_to "Show #{attraction.name}", attraction_path(attraction) if @user.admin %></p>
<% end %>

<p><%= link_to "New Attraction", new_attraction_path if @user.admin %></p>
